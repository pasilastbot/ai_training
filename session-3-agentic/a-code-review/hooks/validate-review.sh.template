#!/bin/bash
# validate-review.sh — PostToolUse hook that validates review output format
#
# This hook runs after each agent produces its output.
# It checks that the output follows the expected format.
#
# Usage: Called automatically as a PostToolUse hook
# Input: Agent output (via stdin or file argument)
# Exit 0: Output is valid
# Exit 1: Output is invalid (agent should retry)

OUTPUT_FILE="${1:-/dev/stdin}"

# TODO: Add validation checks

# Check 1: Output contains required sections
# TODO: Verify the review output contains "## Security Review" or "## Quality Review"
# HINT: grep -q "## Security Review\|## Quality Review" "$OUTPUT_FILE"

# Check 2: Findings have severity levels
# TODO: Verify each finding has a severity (Critical/Major/Minor)
# HINT: Check that the table rows contain severity values

# Check 3: Summary includes a verdict
# TODO: Verify the summary has APPROVE / REQUEST CHANGES / BLOCK
# HINT: grep -q "APPROVE\|REQUEST CHANGES\|BLOCK" "$OUTPUT_FILE"

# Check 4: No empty sections
# TODO: Verify no section is empty (header followed immediately by another header)

# If all checks pass:
echo "Review output validated successfully."
exit 0

# If any check fails:
# echo "ERROR: [describe what's missing]"
# exit 1
