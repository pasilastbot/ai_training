#!/bin/bash
# check-security.sh — Scan code for common security vulnerabilities
#
# Usage: ./tools/check-security.sh <file-or-directory>
# Output: JSON array of findings
#
# TODO: Fill in the grep patterns for each vulnerability type

TARGET="${1:-.}"

echo "["

# Pattern 1: SQL Injection — string interpolation in queries
# TODO: Write a grep pattern that finds SQL queries with template literals
# Example match: db.query(`SELECT * FROM users WHERE id = ${id}`)
# HINT: grep -rn 'PATTERN' "$TARGET"
# grep -rn 'YOUR_PATTERN_HERE' "$TARGET" | while read -r line; do
#   echo "  {\"type\": \"sql_injection\", \"location\": \"$line\"},"
# done

# Pattern 2: Hardcoded Secrets — API keys, passwords in source
# TODO: Write a grep pattern that finds hardcoded secrets
# Example match: const API_KEY = 'sk-live-abc123'
# HINT: Look for variable names like API_KEY, SECRET, PASSWORD with string assignments

# Pattern 3: Missing Input Validation — using req.body without validation
# TODO: Write a grep pattern that finds direct req.body usage without validation
# Example match: const { email, password } = req.body;

# Pattern 4: Eval/exec — dangerous code execution
# TODO: Write a grep pattern that finds eval() or exec() calls

echo "  {\"scan_complete\": true}"
echo "]"
